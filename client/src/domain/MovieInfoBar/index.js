import React from 'react';

const MovieInfoBar = () => {
  const movieSelected = {
    title: 'To die',
  };
  const date = {};
  const cinema = {};
  return <div className="movie__bar" />;
};

export default MovieInfoBar;
